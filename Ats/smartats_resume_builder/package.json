{
  "name": "smartats-resume-builder",
  "version": "2.0.0",
  "description": "Enterprise AI-powered ATS Resume Builder with microservices architecture",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:build": "docker-compose build",
    "docker:restart": "docker-compose restart",
    "ai:dev": "cd ai-service && uvicorn app.main:app --reload --port 8000",
    "ai:test": "cd ai-service && pytest tests/ -v",
    "test": "npm run test:backend && npm run ai:test",
    "test:backend": "echo 'Backend tests not yet implemented'",
    "setup": "npm install && cd ai-service && pip install -r requirements.txt",
    "setup:db": "docker-compose up -d postgres && sleep 5 && psql -h localhost -U smartats_user -d smartats -f database/schema.sql"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.2.0",
    "jsonwebtoken": "^9.0.2",
    "openai": "^4.20.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
